import React from 'react'

import {ScreenScaffold} from '@/components/ScreenScaffold'

export function FileManagerScreen(): React.JSX.Element {
  return (
    <ScreenScaffold
      title="Project Files"
      body="Asset import, project packaging, and working-folder mapping will land here once the exact Tuesday asset contract is wired."
    />
  )
}

